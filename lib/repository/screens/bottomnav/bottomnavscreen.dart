import 'package:flutter/material.dart';

class BootomNavScreen extends StatefulWidget {
  @override
  State<BootomNavScreen> createState() => _BootomNavScreenState();
}

class _BootomNavScreenState extends State<BootomNavScreen> {
  int currentIndex = 0;
  List<Widget> pages = [

  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}